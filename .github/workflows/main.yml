name: Analytics Worker Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print message
        run: echo "Build started"
      - name: Build
        run: |
          echo "Building analytics-worker..."
          echo "Build complete"